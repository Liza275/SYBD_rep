@model Orders

@{
    ViewData["Title"] = "Create order";
}
<div class="text-center">
    <h2 class="display-4">Create order</h2>
</div>

﻿<form class="container-fluid" style="max-width: 580px;" asp-action="Create">

    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="form-group">
        <div class="row">
            <label class="col-lg-4 justify-content-between">
                Employee
            </label>
            <div class="col-lg-8">
                <select name="employeeid" class="form-control" asp-items="@(new SelectList(@ViewBag.Employees,"Id", "Fio"))"></select>
            </div>
        </div>
    </div>

    <div class="form-group">
        <div class="row">
            <label class="col-lg-4 justify-content-between">
                Buyer
            </label>
            <div class="col-lg-8">
                <select name="buyerid" class="form-control" asp-items="@(new SelectList(@ViewBag.Buyers,"Id", "NameBuyer"))"></select>
            </div>
        </div>
    </div>

    <div class="form-group row">
        <div class="col-lg-12">
            <button type="submit" style="min-width: 100px" class="btn btn-sm btn-light">
                Create
            </button>
        </div>
    </div>
</form>